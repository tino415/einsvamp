/// <reference types="vite/client" />

/** Injected by vite.config.ts — the commit the bundle was built from. */
declare const __BUILD_SHA__: string;
/** Injected by vite.config.ts — ISO timestamp of the build. */
declare const __BUILD_TIME__: string;
